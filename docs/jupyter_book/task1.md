# Task 1: Repository Cloning and File Discovery

- Đã sử dụng lệnh `git clone --depth 1 https://github.com/huggingface/diffusers.git` để tải mã nguồn nhanh chóng và tiết kiệm dung lượng.
- Viết script Python (`Thi/file_discovery.py`) đếm và liệt kê các file `.py` trong repo diffusers (loại trừ các thư mục tests, docs, setup.py...).
- Số lượng file Python tìm thấy và lưu vào danh sách: 1338 files.

## Reflection
Quá trình tìm file diễn ra suôn sẻ, đã loại bỏ được các file rác nhờ script.
